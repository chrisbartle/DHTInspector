* text=auto

# Never let line-ending conversion near the icons.
*.png     binary
*.ico     binary

# Executed or parsed on Linux; a CRLF here breaks the shebang or the entry.
*.sh      text eol=lf
*.desktop text eol=lf
